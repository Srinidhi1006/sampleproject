openapi: 3.0.1
info:
  title: sample2API
  description: API Definition of sample2API
  version: 1.0.0
servers:
- url: http://localhost:8290/sample2
paths:
  /second:
    get:
      responses:
        default:
          description: Default response
    post:
      requestBody:
        description: Sample Payload
        content:
          application/json:
            schema:
              type: object
              properties:
                payload:
                  type: object
        required: false
      responses:
        default:
          description: Default response
