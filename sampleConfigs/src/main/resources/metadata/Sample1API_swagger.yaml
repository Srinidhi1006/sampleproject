openapi: 3.0.1
info:
  title: Sample1API
  description: API Definition of Sample1API
  version: 1.0.0
servers:
- url: http://localhost:8290/sample1
paths:
  /first:
    get:
      responses:
        default:
          description: Default response
    post:
      requestBody:
        description: Sample Payload
        content:
          application/json:
            schema:
              type: object
              properties:
                payload:
                  type: object
        required: false
      responses:
        default:
          description: Default response
